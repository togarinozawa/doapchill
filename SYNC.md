# 端末間の同期 — サーバー側の仕様

Android・Windows・ブラウザ拡張のあいだで、制限の設定と記録を共有するための API。
スキマスのバックエンドに相乗りする前提で書いています。

**この文書はサーバー側に実装してもらう分の仕様です。** クライアント側(ドパチル)は
これに合わせて作ります。

---

## 決めたこと

**同期は設定を配るだけ。取り締まりはネットに依存しない。**

各端末はローカルに持っているルールで判定します。サーバーが落ちても、圏外でも、
機内モードでも、制限はそのまま効きます。同期できなかったときに起きるのは
「別の端末で足したルールがまだ届いていない」だけです。

これは学習予定の連携で決めたのと同じ方針です([INTEGRATION.md](INTEGRATION.md))。
`GET /blocking/now` に判定を委ねると、通信を切れば制限が外れる穴になります。

**記録は端末ごとに分けて持ち、読むときに足す。**

ルールは「最後に書いたものが勝つ」でよいのですが、使用時間は違います。
上書きにすると、スマホで30分・PCで20分使った日の合計が20分になってしまう。
端末ごとの区画に書き、合計は読む側で出します。

---

## 1. クライアント側に必要な変更(こちらでやる分)

同期のために `Rule` に**端末をまたいで一意な ID** を足します。
いまの `id` は Android では Room の自動採番、Windows では自前の連番なので、
別々の端末で作ったルールが同じ番号になります。

```kotlin
data class Rule(
    val id: Long = 0L,          // 端末内の ID。今までどおり
    val uid: String = "",       // 追加。同期の鍵。UUID
    ...
)
```

既存のルールには、初回同期のときに UUID を振ります。

---

## 2. 認証

スキマスと同じアカウント・同じトークンを使います。

```
Authorization: Bearer <スキマスのアクセストークン>
```

ドパチル専用のログイン画面は作りません。**PC 版とスマホ版で同じアカウントに
入っていることが、同期の前提**になります。

---

## 3. エンドポイント

### `POST /dopachiru/sync`

送信と受信を1往復でやります。personal 用途なので分ける利点がありません。

**リクエスト**

```json
{
  "deviceId": "win-desktop-8a3f",
  "deviceKind": "windows",
  "since": 4821,
  "changes": {
    "rules": [
      {
        "uid": "8f14e45f-ce3a-4f2b-9e11-2c0f7a1b3d55",
        "updatedAt": 1786001234567,
        "deleted": false,
        "payload": { "name": "夜は開かない", "enabled": true, "target": {...}, "condition": {...}, "actionId": "block", "actionParams": {...} }
      }
    ],
    "tags": [
      { "uid": "tag:sns", "updatedAt": 1786001200000, "deleted": false,
        "payload": { "tag": "sns", "members": ["com.twitter.android", "chrome.exe"] } }
    ],
    "gates": [
      { "uid": "gates", "updatedAt": 1786000000000, "deleted": false,
        "payload": { "gates": [...] } }
    ],
    "changeRequests": [
      { "uid": "cr-91c2...", "updatedAt": 1786001300000, "deleted": false,
        "payload": { "kind": "UPDATE", "targetRuleUid": "8f14...", "status": "PENDING", "clearedGateKeys": ["cooldown"], "gates": [...], "reason": "..." } }
    ]
  }
}
```

**レスポンス**

```json
{
  "rev": 4907,
  "serverTime": 1786001240000,
  "changes": { "rules": [...], "tags": [...], "gates": [...], "changeRequests": [...] }
}
```

- `since` に前回受け取った `rev` を入れると、**それ以降に他の端末が変えたぶんだけ**返します
- 初回は `since: 0`(全件)
- レスポンスの `changes` には、**送り主自身が今送ったものは含めない**でください
- `rev` はユーザーごとの単調増加。サーバーが書き込みのたびに1つ進めます

### 衝突の解決

同じ `uid` が両側で変わっていたら、**`updatedAt` が大きいほうを採る**。
同値ならサーバー側を残す。

端末の時計がずれていると理屈の上では負けるべきものが勝ちますが、
自分1人の端末どうしなので、これで実害が出る場面はほぼありません。
順番の基準(`since` のカーソル)にはサーバーの `rev` を使うので、
時計がずれていても**取りこぼしは起きません**。

### 削除

行を消さず `deleted: true` で送ります(墓標)。消してしまうと、
オフラインだった端末が「まだある」と思って復活させてしまいます。

墓標は**90日で消してよい**です。それより長くオフラインの端末は無いという前提。

---

### `POST /dopachiru/usage`

使用実績。**端末ごとの区画に上書き**します。

```json
{
  "deviceId": "win-desktop-8a3f",
  "days": [
    { "date": "2026-08-10", "totalMinutes": 187,
      "perApp": { "chrome.exe": 92, "discord.exe": 41 },
      "blockShownCount": 6, "overrideCount": 1 }
  ]
}
```

同じ `deviceId` + `date` が来たら差し替え。他の端末の行には触らない。
直近7日ぶんを毎回送ります(取りこぼしの自己修復)。

### `GET /dopachiru/usage?from=2026-08-04&to=2026-08-10`

```json
{
  "days": [
    { "date": "2026-08-10",
      "byDevice": {
        "win-desktop-8a3f": { "totalMinutes": 187, "perApp": {...}, "overrideCount": 1 },
        "pixel-9-1c4d":     { "totalMinutes": 63,  "perApp": {...}, "overrideCount": 0 }
      } }
  ]
}
```

**合算はクライアントでやります。** サーバーは端末ごとのまま返してください。
「全端末合わせて1日30分」も「PC だけで1日2時間」も、同じデータから出せるようにするためです。

連続達成日数(ストリーク)もクライアント側で出します。同期しません。

---

### `GET /dopachiru/study/windows?from=&to=`

学習予定。スキマスがすでに持っているものなので、**新しく作るのは読み口だけ**です。

```json
{ "windows": [
  { "id": "sched_8842", "startAt": 1786000000, "endAt": 1786003600,
    "title": "数学 演習", "goalId": "goal_17", "kind": "study" }
] }
```

形は端末内ブロードキャストの payload と同じにしてあります
([INTEGRATION.md](INTEGRATION.md) 参照)。Android は今までどおり
ブロードキャストで受け取り、**PC 版はこの API から取ります**。

PC にはスキマスが入っていないので、ここだけは通信が要ります。
ただし取れなかったときは「学習予定が無い」扱いになる — つまり**制限が緩む方向**なので、
学習中の制限を PC で使うなら、通信できないときの挙動は許容してください。

---

## 4. 同期のタイミング(クライアント側)

| いつ | 何を |
|---|---|
| 起動時 | sync + usage GET |
| ルールを変えたとき | sync(即時) |
| 15分おき | sync |
| 1時間おき | usage POST |
| 学習予定は 5分おき | study/windows GET(PC 版のみ) |

失敗したら次の機会まで黙って待ちます。リトライで急がせません。

---

## 5. 最低限これだけあれば動く

全部いっぺんに要りません。この順で足してもらえると、こちらも段階的に繋げます。

1. **`POST /dopachiru/sync` の rules だけ** — これでルールが端末をまたぎます
2. tags / gates / changeRequests を同じ枠に足す
3. usage の POST / GET
4. study/windows の GET

---

## 6. 壊れ方

| 起きること | 結果 |
|---|---|
| サーバーが落ちている | 各端末はローカルのルールで動き続ける。制限は外れない |
| 圏外・機内モード | 同上 |
| トークンが切れた | 同上。次にログインし直したときに追いつく |
| 2台で同じルールを同時に編集 | `updatedAt` が新しいほうが残る。古いほうの変更は失われる |
| 片方の端末の時計が大幅にずれている | そちらの編集が不当に勝つ/負ける。カーソルは `rev` なので取りこぼしは無い |
| 片方でルールを消し、もう片方で編集 | `updatedAt` が新しいほうが勝つ(消したほうが後なら消える) |
