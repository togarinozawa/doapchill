# 学習予定との連携

外部アプリから「いまは学習中」を伝えて、そのあいだドパチルに制限をかけさせるための口です。
ver.0.4 から。

- 受け口: [`StudySyncReceiver`](app/src/main/kotlin/com/dopachiru/service/StudySyncReceiver.kt)
- 保管: [`StudyWindowRepository`](app/src/main/kotlin/com/dopachiru/data/StudyWindowRepository.kt)
- 条件: [`StudySessionCondition`](core/src/main/kotlin/com/dopachiru/core/condition/types/StudySessionCondition.kt) (`study_session`)

---

## 決めたこと

**端末内で完結する。** ドパチルに `INTERNET` 権限はありません。サーバー経由にすると
「機内モードにすれば制限が外れる」「サーバーが落ちたら制限が消える」という抜け道が
生まれます。通信できないアプリであること自体が保証になっています。

**窓は自分の終わりを知っている。** 各予定は開始と終了の時刻を持ちます。送り主が落ちて
「終わった」の連絡が来なくなっても、時刻が過ぎれば勝手に解けます。**閉じ込められる方向には
壊れません。**

**全置換。差分ではない。** 届いた配列がそのまま今の全予定になります。届く順番を気にせず、
最後の1通が常に正しい状態になります。解除専用の経路を作っていないのも同じ理由です
(経路が2本あると「終了だけ届かなかった」状態が生まれる)。

---

## 1. 権限

ドパチル側に宣言済み:

```xml
<permission
    android:name="com.dopachiru.permission.SYNC_STUDY"
    android:protectionLevel="signature" />
```

送り主のマニフェストに必要:

```xml
<uses-permission android:name="com.dopachiru.permission.SYNC_STUDY" />
```

`signature` 保護なので、**ドパチルと同じ鍵で署名したアプリしか名乗れません。**
両方とも debug キーストア (`~/.android/debug.keystore`) で署名していれば通ります。
鍵が違うとインストールはできますが、ブロードキャストは黙って捨てられます。

---

## 2. 送り方

```kotlin
val payload = """
{
  "version": 1,
  "windows": [
    {
      "id": "sched_8842",
      "startAt": 1786000000,
      "endAt": 1786003600,
      "title": "数学 演習",
      "goalId": "goal_17",
      "kind": "study"
    }
  ]
}
"""

val intent = Intent("com.dopachiru.action.SYNC_STUDY_WINDOWS").apply {
    setPackage("com.dopachiru")        // ← 必須
    putExtra("windows", payload)
}
sendBroadcast(intent)
```

**`setPackage` は必須です。** Android 8 以降、宛先を指定しないブロードキャストは
マニフェスト登録の受信機に届きません。付け忘れると、エラーも出ずに何も起きません。

### フィールド

| | 型 | 必須 | 内容 |
|---|---|---|---|
| `id` | string | ○ | 送り主が付ける識別子。空だとその窓は捨てられる |
| `startAt` | number | ○ | 開始。**UTC のエポック秒**(ミリ秒ではない) |
| `endAt` | number | ○ | 終了。`startAt` 以下だとその窓は捨てられる |
| `title` | string | | ブロック画面などに出す名前 |
| `goalId` | string | | 保存はするが、いまのドパチルは見ていない |
| `kind` | string | | 同上 |

`goalId` と `kind` を受け取って捨てずに置いているのは、将来「数学の予定のときだけ
辞書アプリを許す」のように窓ごとに許可リストを変えたくなったときのためです。
ver.0.4 は**学習中かどうかだけ**を見ます。

壊れた JSON が来ても落ちません。読めなければ何もせず無視します(前の状態が残る)。

---

## 3. いつ送るか

| 場面 | 送るもの |
|---|---|
| 起動時・予定を組み直したとき | 今日(必要なら明日も)の全窓 |
| 予定の開始時刻 | 変わっていなければ送り直さなくてよい |
| **早く終わった → いま解除したい** | **その窓の `endAt` を現在時刻にして、全窓を送り直す** |
| 予定が消えた | その窓を除いた全窓 |

窓に未来の予定を含めて先に送っておくのが最も安全です。開始時刻に何も届かなくても、
ドパチルは自分で時刻を見て制限を始めます。

---

## 4. 反応の速さ

- **受信した瞬間に判定をやり直します。** 学習開始のとき、対象アプリはもう前面にいて
  画面切り替えのイベントが来ないので、これが無いと最大30秒(電池優先モードなら2分)待たされます
- 窓の**境界の時刻ちょうど**に自動で切り替わります。ドパチルは次の境界まで一度も起きず、
  その時刻に起きるよう予約します(電池を使いません)
- ユーザー補助が一時的に落ちていても、受信自体はマニフェスト登録なので取りこぼしません。
  受け取った時点で Room に入るので、次にサービスが立ち上がったとき学習中のまま復元されます
- 端末を再起動しても、窓は Room から読み直されます

---

## 5. ドパチル側の設定

ユーザーがルールを1つ作ります。**連携をオンにしただけでは何も起きません**
(自動生成にすると、ユーザーが編集・削除したときの同期問題が生まれるため)。

雛形「**学習中は必要なアプリだけ**」を選ぶと1タップで作れます。中身は:

```
対象   : 全アプリ − 選んだアプリ(許可リスト型)
条件   : study_session = 予定の最中
アクション: 完全封印 / 押し切り不可
```

電話・ホーム・設定・キーボード・ドパチル自身は、選ばなくても必ず使えます
([`ProtectedApps`](app/src/main/kotlin/com/dopachiru/data/ProtectedApps.kt))。
ここはルールより強く、設定画面からも外せません。

**学習予定の最中は押し切れません。** ルールの「押し切って使えるようにする」設定に関わらず
ブロック画面の逃げ道が消え、予定が始まる前に押し切っておいた猶予も持ち込めません。

---

## 6. テストするときの注意

`adb shell am broadcast` では送れません。shell はこの署名権限を持てないので、
Permission Denial になります。これは設計どおりです。

手元で確かめるには、次のどちらか:

1. スキマス側の実装から送る(本番と同じ経路)
2. 一時的に [`AndroidManifest.xml`](app/src/main/AndroidManifest.xml) の
   `StudySyncReceiver` から `android:permission` の行を消してビルドし、adb から叩く。
   **確認が済んだら必ず戻すこと**

受信できたかどうかは、ルール一覧のルールが反応するかで見るのが早いです。

---

## 7. 壊れ方の一覧

| 起きること | 結果 |
|---|---|
| スキマスのプロセスが死ぬ | 窓の終了時刻で自動的に解ける |
| 機内モード / 圏外 | 影響なし(通信していない) |
| ブロードキャストが届かない | 制限がかからない(**かかりっぱなしにはならない**) |
| 壊れた JSON | 無視。前の状態が残る |
| 鍵が違うアプリから送られた | 届かない |
| 端末再起動 | Room から復元 |
| ユーザー補助が切られている | 制限はかからない。受信と保存は続く |
