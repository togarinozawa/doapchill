<?xml version="1.0" encoding="utf-8"?>
<!--
  flagRetrieveInteractiveWindows と flagIncludeNotImportantViews は付けていない。
  どちらもシステム側にノードツリーを余分に組ませるぶん常時の負荷になるが、
  このアプリが使うのは rootInActiveWindow だけで、それには canRetrieveWindowContent で足りる。

  notificationTimeout は 0 のまま。ここを伸ばすとイベントの配送が遅れ、
  ブロック画面が出るまでに対象アプリの中身が見えてしまう。
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="0"
    android:settingsActivity="com.dopachiru.MainActivity" />
