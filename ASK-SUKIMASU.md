# スキマス側への質問と要望

ドパチル(自己制限アプリ)側から、スキマス側の担当へ。
連携は2つあります。

1. **学習予定の連携** — 端末内ブロードキャスト。仕様は [INTEGRATION.md](INTEGRATION.md)(実装済み・未接続)
2. **端末間同期** — スキマスのバックエンドに相乗りさせてもらう。仕様は [SYNC.md](SYNC.md)(これから)

この文書は主に **2 を進めるために必要なこと**です。

---

## A. 答えてほしいこと(サーバー)

これが分かると、仕様書ではなく**動くサーバーコードを書けます**。

| # | 質問 | なぜ要るか |
|---|---|---|
| A-1 | バックエンドの言語とフレームワーク | 同じ流儀でルーターを書くため |
| A-2 | どこで動いていますか(VPS / PaaS / その他、サービス名) | デプロイの手順に合わせるため |
| A-3 | 認証はどの方式ですか(セッション Cookie / JWT / API キー) | ドパチルのクライアントをそれに合わせる |
| A-4 | トークンの寿命と、リフレッシュの仕組みはありますか | 期限切れのときの再取得を実装するため |
| A-5 | DB は何で、マイグレーションはどう回していますか | テーブル追加のやり方を合わせるため |
| A-6 | ユーザーIDの形(整数の連番? UUID? 外部IdP のsub?) | 同期データの持ち主を表す鍵になる |

## B. 答えてほしいこと(既存の実装)

| # | 質問 |
|---|---|
| B-1 | `GET /blocking/now` の**実際のレスポンス**を1つ貼ってください。認証ヘッダも含めて |
| B-2 | 学習予定のデータモデルを教えてください。`goal_id` は何を指しますか |
| B-3 | 「種別」(kind)の取りうる値を全部挙げてください |
| B-4 | 予定はサーバーが正で、端末はそれを写しているだけですか。それとも端末側でも編集できますか |

## C. 判断してほしいこと

| # | 質問 | ドパチル側の推し |
|---|---|---|
| C-1 | ドパチル用のルートを**既存アプリに足して**いいですか。それとも別サービスにしますか | **既存アプリに `/dopachiru/*` を足す**。認証を作り直さずに済み、デプロイも1つのまま |
| C-2 | テーブルは分けてよいですか(`dopachiru_` 接頭辞 or 別スキーマ) | **分けたい。** 片方のマイグレーションでもう片方が壊れないように |
| C-3 | レート制限や課金上限に、同期のトラフィックを載せて問題ありませんか | 同期は数分おきに小さな JSON を1往復するだけ。スキマス本体と比べれば誤差のはず |

---

## D. 実装してほしいもの(優先順)

全部いっぺんには要りません。**1 だけあれば繋ぎ始められます。**

### 1. `POST /dopachiru/sync` の rules だけ

これでルールがスマホと PC のあいだを行き来します。ここまでで同期の価値の8割です。

- リクエスト・レスポンスの形は [SYNC.md](SYNC.md) の「3. エンドポイント」
- 衝突は `updatedAt` が大きいほうが勝つ
- 削除は行を消さず `deleted: true`(墓標)。消すと、オフラインだった端末が復活させてしまう
- `since` に渡すカーソルは**サーバーが振る `rev`**。端末の時計に依存させない

### 2. tags / gates / changeRequests

同じ `POST /dopachiru/sync` の枠に足すだけです。形は rules と同じ
(`uid` / `updatedAt` / `deleted` / `payload`)。

### 3. `POST` `GET /dopachiru/usage`

使用実績。**端末ごとの区画に持ってください。** 上書きにすると、
スマホで30分・PC で20分使った日の合計が20分になります。合算は読む側でやります。

### 4. 学習予定の端末内ブロードキャスト(Android のスキマス側)

[INTEGRATION.md](INTEGRATION.md) の通り。実装するときの落とし穴が2つあります。

- **`intent.setPackage("com.dopachiru")` が必須。** Android 8 以降、宛先の無い
  ブロードキャストはマニフェスト登録の受信機に届きません。付け忘れるとエラーも出ずに無反応
- **`<uses-permission android:name="com.dopachiru.permission.SYNC_STUDY" />` が必要。**
  signature 保護なので、両アプリを**同じ鍵で署名**してください。鍵が違うとインストールは
  できますがブロードキャストは黙って捨てられます

### 5. `GET /dopachiru/study/windows` — **保留**

PC 版スキマスを Web で作る方針とのことなので、いったん止めています。
そちらができたら再開します。

---

## E. 保留・見送り

| | 状態 |
|---|---|
| PC 版スキマスとの連携 | Web 版ができてから |
| `goal_id` / `kind` を使った出し分け | 受け取って保存はしているが、まだ見ていない。「数学の予定のときだけ辞書を許す」は窓ごとに許可リストを変える設計になるので、いまは「学習中かどうか」だけ |
| サーバーに判定を委ねる (`/blocking/now` の直接利用) | **やりません。** 通信を切れば制限が外れる穴になるため。同期は設定を配るだけで、判定は各端末がローカルでやります |

---

## F. ドパチル側の状態(参考)

| | |
|---|---|
| Android | ver.0.4.1。ルールエンジン、条件11種、アクション3種、ゲート6種、学習予定の受信口まで実装済み |
| Windows | Compose Desktop 版。前面アプリ検知・3段階のブロック・トレイ常駐まで実機で動作確認済み |
| ブラウザ拡張 | 未着手。同期のあとに作ります |
| テスト | 46件(ルールエンジン・ゲート・スケジューリング・学習予定の窓) |

ルールエンジンは `:core` にあり、**Android と Windows で同じコードが動いています**
(Android の import が1つも無く、`java.time` と kotlinx.serialization だけで書かれている)。
